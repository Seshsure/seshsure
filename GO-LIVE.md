# SESHSURE HUB — GO-LIVE RUNBOOK
_One sitting, ~45 minutes of account creation. Everything under rob@seshsure.com._

## STEP 1 — Accounts to create (in this order)
| # | Service | Plan | Cost | What to grab |
|---|---------|------|------|--------------|
| 1 | **GitHub** — github.com | Free | $0 | org name (suggest: `seshsure`) |
| 2 | **Supabase** — supabase.com | Pro | $25/mo | Project URL, anon key, service-role key (Settings → API) |
| 3 | **Vercel** — vercel.com | Pro | $20/mo | just sign in with the GitHub account |
| 4 | **Resend** — resend.com | Free tier to start | $0 | API key; add domain seshsure.com |
| 5 | **EasyPost** — easypost.com | Pay-as-you-go | ~$0 | API key |
| 6 | **1Password** — 1password.com | Business | $8/mo | vault named `SeshSure Hub` |

Rules while creating:
- **Same email everywhere: rob@seshsure.com** (accounts are company assets — diligence checks this)
- Turn on 2FA on all six, TOTP codes stored in 1Password
- Every password: generated by 1Password, saved to the `SeshSure Hub` vault

## STEP 2 — DNS (at your domain host, ~10 min)
- `hub.seshsure.com` → CNAME → `cname.vercel-dns.com`
- Resend will show 3 records (SPF/DKIM) when you add seshsure.com — paste them in
- These make hub live + emails land in inboxes instead of spam

## STEP 3 — Hand me the keys
Paste into our chat (or 1Password-share):
1. Supabase: project URL + anon key + service-role key
2. Resend API key
3. EasyPost API key
4. GitHub org name (I give you the repo push commands)

## STEP 4 — I take it from there (same day)
- Push repo → Vercel import → env vars in → deploy
- Run migrations 0001–0009 against Supabase
- Seed: Vido entity, freight lanes, counters, your owner login
- Smoke test: login → signup wizard → invoice → pay (sandbox) → batch tap → cron fire
- Hand you your login + the first daily brief arrives next 8 AM

## LATER (not blockers)
- First Citizens ACH file credentials → ODFI_ROUTING + company ID (activates direct rail)
- Merchant platform key → PAYMENT_RAIL=processor (if meeting wins)
- QuickBooks CSVs → history import → penny-perfect reconciliation → QB dies
